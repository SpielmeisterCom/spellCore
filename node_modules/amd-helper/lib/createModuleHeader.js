module.exports = function( name ) {
    var parts = [
        "define(",
        "	'" + name + "',",
        "	[",
		"		'spell/functions'",
		"	],",
		"	function(",
		"		_",
		"	) {",
		"		// all the codes belongs to here",
		"	}",
		")"
    ]

	return parts.join( "\n" )
}

