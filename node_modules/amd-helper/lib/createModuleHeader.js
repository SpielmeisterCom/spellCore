module.exports = function( name ) {
    var parts = [
        "define(",
        "	'" + name + "',",
        "	[",
		"		'spell/shared/util/platform/underscore'",
		"	],",
		"	function(",
		"		_",
		"	) {",
		"		// all the codes belongs to here",
		"	}",
		")"
    ]

	return parts.join( "\n" )
}

